# Runtime secrets

Create these files locally before starting the production Compose project:

- `youtrack-sanitized.token`
- `youtrack-plain.token`
- `sanitizer-pseudonym.key`

They are ignored by Git. The `*.example` files document the expected format.
